# AI 学习电台｜技术适配声明

> 日期：2026-08-21
>
> 配套文档：《AI学习电台-MVP需求总结》《AI学习电台-架构推导方案》《AI产品Vibe Coding通用技术栈手册》
>
> 适用范围：MVP 开工前与第一阶段“私人学习播客最小闭环”

## 1. 产品形态判断

- **产品类型与核心任务**：移动端优先的 AI 内容转音频学习产品。用户通过邀请码进入，把私人学习资料转换为中文教学播客；系统还会按频道生产可追溯来源的共享新闻节目。
- **核心交互**：固定流程的一次生成 + 异步任务状态 + 生成后的收听、知识点查看和主动回忆。它不是开放式对话，也不需要模型自主规划工具。
- **编排判断**：固定流程由代码状态机编排，模型只负责筛选、归纳、写稿等内容节点，即“脚本决定编排，模型决定单步”。
- **开发路径**：采用**最小纵向切片**。私人学习播客的价值必须通过真实上传、等待状态、音频播放、文字稿和回忆题一起判断，仅验证后端 JSON 不足以判断产品是否成立。
- **当前阶段范围**：只完成“邀请码进入 → 上传一份 DOCX/PDF/Markdown → 填写学习重点 → 从 3 个预设音色中选择并试听 → 异步生成 → 收听节目 → 查看知识点和回忆题 → 删除个人数据”的最小闭环。
- **本阶段暂不包含**：每日新闻、公开文章链接、正式云部署、管理员网页后台、社交分享、多文件处理、OCR、开放问答和多终端客户端。

## 2. 采用的默认方案

- **Python 3.11**：符合项目默认基线；建议通过 uv 或其他项目级版本管理工具安装与隔离环境。
- **FastAPI**：承载认证、上传、任务查询、节目读取、删除和事件上报 API。
- **Pydantic**：校验 API 输入输出以及 LLM 结构化结果。
- **pytest**：完成离线 mock 自动化测试和业务状态回归。
- **Next.js + TypeScript + App Router + Tailwind CSS**：目标终端是移动端优先的 Web/PWA，第一阶段只做核心纵向切片所需页面。
- **统一 `/api/v1` API 前缀**：新项目直接采用版本化接口。
- **代码状态机**：两条业务管线均为固定工作流，不引入动态 Agent。
- **提交任务 + 轮询状态**：生成耗时长且不需要逐字展示内容，轮询比 SSE 更贴合产品交互。
- **Prompt 独立文件与版本号**：Prompt 不散落在路由或页面代码中，生成结果可追溯到 Prompt 版本。
- **主 LLM：火山方舟 `doubao-seed-2-1-pro-260628`**：使用固定版本，关闭深度思考，通过 JSON Schema 生成结构化教学内容；第一阶段所有 LLM 内容节点先统一使用该模型，减少变量。
- **TTS：豆包语音 `seed-tts-2.0`**：使用异步长文本合成和 MP3 输出；私人节目允许选择 3 个白名单系统音色，新闻固定使用默认品牌音色。
- **单体 API + 独立 Worker 进程**：保持部署简单，同时避免长任务阻塞 Web 请求。

## 3. 触发的按需模块

| 模块 | 触发需求 | 当前阶段处理 |
| --- | --- | --- |
| SQLite + SQLAlchemy + Alembic | 用户、凭证、任务、节目、额度、事件存在关系和事务 | 引入；本地阶段使用 SQLite，模型保持可迁移到 PostgreSQL |
| 持久化任务记录 + Worker | LLM/TTS 是长耗时任务，必须支持重试和进程重启恢复 | 引入；先用数据库任务表和 Worker 轮询，不引入重型消息队列 |
| 本地受控文件目录 | 上传文件、生成音频属于大二进制资产 | 引入；只保存受控路径和元数据，原文件解析成功后删除 |
| `python-docx` | PRD 要求支持 `.docx` | 引入 |
| `pypdf` | PRD 要求支持文本型 PDF | 引入 |
| Markdown 文本解析 | PRD 要求支持 `.md` | 引入轻量解析，不增加复杂渲染系统 |
| LLM Provider 适配层 | 已确认火山方舟为第一阶段供应商，且需要 mock 与真实实现可替换 | 引入；主模型固定为 `doubao-seed-2-1-pro-260628` |
| TTS Provider 适配层 | 已确认豆包语音 `seed-tts-2.0`，且 TTS 失败要能降级为文字稿 | 引入；使用受控音色目录，不接受任意供应商音色 ID |
| Provider 用量账本与预算熔断 | LLM/TTS 会产生真实费用，PRD 要求限额与总预算保护 | 引入；每次调用前检查预算，调用后记录实际或估算用量 |
| 最小 PWA 前端 | 移动上传、异步状态和音频播放必须通过真实交互验收 | 引入；不提前实现完整四页信息架构 |

以下模块当前**不引入**：OCR、Chroma/RAG、Embedding、动态工具选择、MCP、多 Agent、Redis/Celery、WebSocket、SSE、微服务和容器编排。

## 4. 偏离或暂缓的默认方案

- **Node.js 22 LTS 或更新兼容版本**：优先使用 LTS；使用更新版本时先验证 Next.js 依赖兼容性。项目需声明支持的 Node 版本并提交锁文件。
- **生产 PostgreSQL/对象存储 → 第一阶段 SQLite/本地私有目录**：先验证核心内容链路，减少外部平台依赖。数据库模型、对象键和存储接口必须从第一天保持可迁移；进入真实多用户上线阶段前迁移到 Supabase PostgreSQL + 私有 Storage，并验证 RLS。
- **完整管理员后台 → 第一阶段管理员 CLI/种子脚本**：邀请码生成和测试用户初始化先通过受控命令完成；需要非技术人员频繁管理用户时再增加后台页面。
- **正式四页 PWA → 第一阶段最小纵向切片**：只实现登录、上传、预设音色试听与选择、任务状态、节目结果和个人数据删除；核心链路验证通过后再按正式前端手册扩展“今日/学习/节目库/我的”。
- **真实模型与 TTS → 开发期 provider mock**：LLM 与 TTS 均已选定火山引擎，但当前没有发现已配置的相关环境变量。没有 Key 不阻塞开发，但真实模型和语音的端到端冒烟未通过前，不能宣称阶段验收完成。
- **开发端口 3001/8001**：作为项目约定的本地默认值；如有冲突，可通过环境变量或启动参数覆盖。

## 5. 强制底线检查

- **密钥与隐私：待实现**
  真实 Key 只允许进入后端 `.env` 或部署平台 Secret；仓库只提交 `.env.example`。日志不得记录 Key、完整私人文档或完整 Prompt 输入。原始上传文件解析成功后立即删除。
- **数据与任务可恢复：待实现**
  用户、额度、任务状态、步骤结果、Provider 用量和生成节目均持久化。Worker 重启后可从最后一个成功步骤继续，不重复扣减额度和重复执行已完成的高成本步骤。
- **输入输出校验：待实现**
  上传文件同时校验扩展名、MIME、文件头、大小和安全文件名；提取正文不得超过约 3 万字。LLM 输出经过 Pydantic 校验，失败时有限重试。
- **错误与日志边界：待实现**
  API 使用统一错误结构；用户只看到可操作的中文错误，服务端记录任务 ID、步骤、耗时和错误类型，不向前端泄露堆栈。
- **测试与真实模型验收：计划已明确**
  每次开发完成运行 mock 自动化测试；阶段验收前，每种 LLM/TTS 契约至少完成一次真实输入到持久化和用户可见结果的端到端冒烟，并记录模型、耗时、Token/字符量、估算成本和结果质量。
- **Git：发布要求已明确**
  公开仓库必须从完成净化的代码副本初始化全新历史，不继承含第三方材料的旧历史；`.gitignore` 统一保护凭证、数据库、上传文件、生成音频、日志、缓存与私有素材。协作采用 `main` 稳定基线 + 短期 `feature/*` 分支。

### 5.1 通用开发环境要求

| 检查项 | 通用要求 | 处理 |
| --- | --- | --- |
| 代码来源 | 使用完成授权与安全检查的开源副本 | 不复制或合并旧仓库历史 |
| Git | 使用支持当前仓库格式的稳定版本 | 发布时初始化全新历史，稳定基线保存在 `main` |
| Python | 需要 Python 3.11 与 uv | 使用项目级虚拟环境，不依赖系统 Python |
| Node/npm | 推荐 Node.js 22 LTS 与其配套 npm | 使用锁文件安装依赖；更新运行时后重新执行测试与构建 |
| 配置文件 | 仓库只提交安全的 `.env.example` | 本地及部署环境通过未跟踪文件或 Secret 管理服务注入配置 |
| 模型/TTS Key | 开发默认使用 mock | 真实验收前由授权人员通过 Secret 管理服务配置 |
| 端口 | 前端默认 3001，后端默认 8002 | 如有冲突，通过环境变量或启动参数覆盖 |

## 6. 需要产品经理决定的问题

LLM 已确认采用火山方舟 `doubao-seed-2-1-pro-260628`。第一阶段关闭深度思考并使用 JSON Schema；`deepseek-v4-flash` 只作为后续 A/B 对照模型，不做运行时自动切换。TTS 已确认采用豆包语音 `seed-tts-2.0`，私人节目支持 3 个预设音色，新闻固定“儒雅青年 2.0”。真实验收前需安全配置火山方舟与豆包语音所需凭证。

以下问题**不阻塞第一阶段的 mock 开发**，但会影响真实验收、费用或第三方数据处理，必须在真实冒烟前确认：

1. **真实测试资料**：真实冒烟需要一份不含敏感信息、允许发送给第三方模型与语音服务的 DOCX/PDF/Markdown 示例。
2. **本项目月度测试预算**：需要从“与 AI 相册合计每月不超过 1000 元”中为本项目明确一个上限；在上限未配置前，真实 Provider 调用默认关闭。

以下事项推迟到对应阶段再确认，不阻塞当前阶段：可信新闻源清单、正式部署平台、Supabase 项目和正式域名。
