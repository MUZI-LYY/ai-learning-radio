# 安全策略

## 支持范围

项目目前处于早期公开阶段，只为 `main` 分支最新版本提供安全修复。旧提交、个人 Fork 和未经维护者发布的部署不在支持范围内。

## 私下报告漏洞

请使用 GitHub 仓库 **Security → Report a vulnerability** 的 [Private Vulnerability Reporting](https://github.com/liyongyan129-maker/ai-learning-radio/security/advisories/new) 提交报告。

请不要在公开 Issue、Discussion、Pull Request、社交媒体或演示站点中披露漏洞细节，也不要提交真实用户数据或有效凭证。报告建议包含：

- 受影响的提交、版本、接口或组件。
- 风险、攻击前提和可能影响。
- 使用虚构数据的最小复现步骤或测试用例。
- 可行的缓解或修复建议（如有）。

维护者会尽力在 7 天内确认收到报告，并在完成初步评估后更新处理计划。项目由小规模维护者维护，修复时间会随复杂度变化；本说明不是服务等级承诺。

## 安全测试边界

- 只测试你拥有或已获得明确授权的本地实例。
- 不访问、修改或删除他人数据，不进行拒绝服务、社工、垃圾信息或供应链攻击。
- 不使用真实 Provider 凭证进行概念验证；优先使用 mock 模式和虚构内容。
- 如果测试可能产生第三方 LLM/TTS 费用或外传文本，请先停止并改用本地复现。

## 部署方责任

项目采用本地单用户模式，不内置登录与访问控制。对公网开放前，请在反向代理或网关层增加身份认证、启用 HTTPS，把凭证放入 Secret 管理服务，并评估数据库、对象存储、备份、日志脱敏、速率限制和数据删除策略。应用内预算熔断不替代 Provider 侧的额度、账单告警或组织策略。
