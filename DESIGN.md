# AI 学习电台视觉规范

版本：4.1（2026-08-24）
适用范围：移动端 Web / PWA，优先宽度 320–500px。

## 1. 已确认方向

“每日资讯”是一台轻量、可阅读的 AI 新闻电台。首屏只服务四件事：看见今日节目、开始播放、切换三个频道、跟随口播阅读。界面采用浅蓝原生电台感，蓝色负责播放与选中状态，紫色只用于玻璃文字稿中的 AI 信号。

页头仅保留“每日资讯”，不显示日期、头像或设置。底部 Dock 固定为三项：每日资讯、个人节目、我的。

## 2. 视觉语言

- 背景：浅蓝到近白、淡紫的纵向渐变，顶部可有克制的蓝色光晕。
- 主要操作：高饱和蓝 `#168CFF`；文字稿强调：紫色 `#9C4DFF`。
- 正文：深蓝黑 `#151B2B`；辅助信息：蓝灰 `#667085`。
- 卡片：半透明白色、细白描边、柔和蓝灰阴影；图片使用中性描边，不叠加彩色滤镜。
- 排版：采用 28 / 24 / 20 / 18 / 17 / 16 / 14 / 12px 移动端阶梯；时间与序号使用等宽数字特性。依据 Apple Dynamic Type 的 17pt 正文基线与 Material 3 的 16px Body Large、22px Title Large、24px Headline Small收敛，中文正文不再使用 10px 以下字号。
- 全站页面级标题统一使用 28px；详情页主标题使用 24px；区块标题与详情页顶部导航使用 18px；正文和主要表单标签使用 16px；说明、次要按钮文字使用 14px；来源标签、日期、时间和导航标签使用 12px。登录页品牌标题可保持 24px，长文正文为保证连续阅读可使用 17px。
- 动效：仅对具体属性做 140–240ms 过渡；遵循 `prefers-reduced-motion`。

## 3. 每日资讯页

页面信息顺序固定为：

1. “每日资讯”标题，不显示日期。
2. 三张频道节目卡组成循环横滑轮播；“每日资讯”标题底部到中间卡片上边缘固定为 20px，卡片、小猫与两侧卡片作为整体上移。三张卡片统一使用无彩色的半透明白色毛玻璃背景。当前卡居中并接近容器满宽；左右相邻频道只露出窄边作为可滑动提示，并使用 96% 缩放与 68% 不透明度做轻度弱化。
3. 节目标题直接使用第一条 AI 核心观点的一句话版本；卡片内的频道名为 20px，并单独向上微调 6px。当前卡片右上边缘放置透明背景的 3D AI 机器人猫，双爪压住卡片边缘。主图常规屏幕为 80px、小屏为 72px；图片右侧标题使用 16px、600 字重，最多显示两行，超出用省略号；“查看更多”移动到两行标题下方。主图本身不再叠加播放按钮。
4. “每日资讯”标题的左边缘与中央活动卡片左边缘对齐；标题、节目卡片与齿轮频道区共用一段连续的浅蓝渐变背景，不在卡片下方切换底色，避免上下区域割裂。毛玻璃效果集中应用在节目轮播卡片和底部导航：半透明白色渐变、柔和描边、背景模糊与轻微饱和度增强。齿轮和文字稿继续直接落在页面背景上，避免卡片层级过多。
5. 卡片下方使用 SVG 绘制的弧形齿轮：三个频道分类统一使用 16px 常规字重，当前频道仅用品牌蓝区分；淡蓝弧线、沿弧线分布的长短刻度、中央蓝色指针。仅弧线和刻度纵向镜像；刻度组从上移 15px 的位置回落 4px，最终相对初始位置上移 11px；指针自身向上移动其长度的 3/4（22.5px），始终向上指向中央当前频道。
6. 当前中央卡片在未播放时也常驻显示控制区：图文内容下方不使用分隔横线，直接展示进度控件；同一横行从左到右为已播时间、进度条和节目总时长；下一行使用三列对称布局，左侧显示用户指定的爱心收藏图标（未收藏镜空、已收藏实心），中央使用用户指定且造型配套的实心圆形播放/暂停 SVG（圆形属于图标自身，按钮不再叠加额外背景），右侧显示设置图标。播放控件位于卡片几何中心，收藏和设置的视觉边缘分别与控制区左右边界对齐。播放与暂停图标均为 48px 并使用唯一主题蓝 `#168CFF`，两侧图标为 26px 深色 `#182033`，通过无障碍标签补充操作名称。不再显示悬浮迷你播放器。
7. 文字稿直接排在页面背景上，背后居中叠加低透明度的浅蓝与偏暖浅紫柔光光晕，以浅紫为主、浅蓝为辅，效果保持克制。引号、日期/段落序号、内容标签、正文和底部分段线统一收进与上方中间卡片相同的左右边界。装饰引号使用 100px；引号保持不动，日期/段落序号、内容标签和正文整体向上移动 20px；正文相对日期与标签再向下留出 8px 间距。日期/段落序号和内容标签统一为 14px。首段标签由当前节目的 `channel_name` 自动生成“频道名 · 核心观点”，分别展示“AI 前沿 · 核心观点”“创业商业 · 核心观点”“科技产品 · 核心观点”，避免不同频道都显示 AI 标签。核心观点与后续每条新闻使用完全一致的正文结构：不显示 `program.title` 或 `item.title`，只显示 `program.summary` 或 `item.narration`；所有正文统一为 24px 深色加重样式。中文正文使用字符级两端对齐，统一每行左右边缘，末行保持左对齐；超长英文或连续字符允许安全换行。段落进度线和当前字使用同一品牌蓝。
8. 三项底部 Dock。

最终精修统一补充以下规则：活动卡片使用分层蓝灰阴影与顶部内高光，图片增加中性内描边；活动卡片底部内边距增加 4px，使整体高度增加 4px；卡片与齿轮之间只保留必要的阴影呼吸空间；短标题使用更自然的换行策略；长频道标签限制宽度并右对齐，避免与段落序号碰撞；播放/暂停图标切换采用轻微淡入缩放，收藏加载态和主要按钮保留明确的触控反馈。

当前逐字跟随是基于口播字符长度的均匀时间估算，不宣称为 TTS 返回的真实字级时间戳。

## 4. 来源与阅读

点击“查看更多”进入本期来源页，展示约 6 篇文章卡片。页面顶部“本期来源”使用 18px 导航标题，节目标题使用 24px、节目摘要使用 16px；节目摘要下方以浅蓝胶囊展示频道，日期紧随其后，来源总量使用 20px 的“6篇来源文章”整句表达，并与第一张文章卡片保持 20px 间距。每张卡片保留左侧主图（无图时显示序号占位），右侧标题使用 16px、摘要使用 14px，均最多展示两行并在超出时省略。卡片底部以 12px 浅蓝胶囊展示文章来源，胶囊与右侧 12px“阅读全文”入口沿同一水平中线对齐；交互区域高度不小于 44px。320px 小屏仅缩小图片和横向间距，不缩小文字。

点击卡片进入站内阅读页：顶部“阅读来源”与“本期来源”统一为 18px；文章来源使用 12px 浅蓝胶囊，与 12px 日期在文章标题上方同一行展示。文章标题使用 24px，摘要使用 16px 并最多展示 5 行，长文正文使用 17px，底部抓取说明和原文入口分别使用 12px、14px。若抓取失败或正文超过 30,000 字符，页尾必须明确提示内容不完整，并提供原文链接；完整正文也始终保留原站入口。

## 5. 播放器

- 主节目卡播放按钮是首屏第一操作，点击目标不小于 44px。
- 播放状态留在顶部卡片内；主按钮、进度、时间、收藏和设置构成一个连续控制面。
- 收藏写入用户账号，并在“个人节目 → 我的收藏”展示，不使用临时浏览器状态。
- 设置面板支持真实 0.75–2x 倍速，默认使用更自然的 1x；当前共享新闻只有一条固定音色音轨，其他音色仅提供试听，完整切换需要多音轨生成方案。
- 切换频道时暂停并重置当前节目，避免多条音轨同时播放。
- 覆盖音频加载失败、节目缺失、频道加载失败和音频尚未准备等状态。

播放设置使用底部抽屉：页面标题 24px、区块标题 18px、音色名称 16px，速度按钮、说明文字、音色描述与试听按钮均为 14px。速度按钮高 48px，试听和关闭按钮不小于 44px。抽屉打开后锁定背景滚动，支持点击遮罩、关闭按钮与 Esc 退出，并把键盘焦点约束在弹层内。

## 6. 个人节目

- 页面标题使用 28px，并始终在首屏展示“生成你的专属节目”入口；入口跳转到资料上传流程，不因已有节目而隐藏。
- 内容区使用“我的节目 / 我的收藏”双标签。无内容时展示明确空态和下一步说明；有内容时使用与每日资讯一致的半透明白色毛玻璃卡片。
- 节目卡片保留 76px 封面区，频道或音色使用 12px，标题使用 16px且最多两行，来源、时长和收藏状态使用 12px；整张卡片均可点击播放，右侧播放图标使用唯一主题蓝。
- “我的节目”读取用户生成的个人节目；“我的收藏”读取每日资讯中已收藏的节目。两类数据共用视觉组件，但保持各自的详情、音频和逐字稿取数链路，避免音频与文字错配。
- 点击卡片后，从底部打开播放抽屉。抽屉优先展示 18px 逐字稿，当前字只改变颜色和背景，不改变字宽与字号，避免正文抖动；底部固定毛玻璃播放卡，包含封面、单行标题、播放状态、进度条和 48px 播放/暂停按钮。
- 抽屉支持遮罩、关闭按钮和 Esc 退出，打开期间锁定页面滚动并约束键盘焦点；320px、375px 和 390px 下不得横向溢出。

## 7. 我的与设置

- “我的”页面使用 28px 页面标题；个人信息卡展示 64px 默认头像、20px 默认昵称“亲爱的用户”和右侧 44px 设置入口。
- 学习数据面板展示生成资料、收听节目与收听分钟。已有可靠接口的数据使用真实值；尚无统计接口时明确展示 0，不伪造行为数据。
- 今日生成额度单独展示，数字使用 18px并采用等宽数字特性。
- 删除全部个人数据和退出登录从主页移至独立设置页。设置页顶部导航、区块标题使用 18px，主要表单文字和危险操作按钮使用 16px，说明文字使用 14px。
- 删除全部个人数据必须输入完整确认短语后才启用按钮；该操作不可恢复，视觉测试和普通验收不得实际触发。

## 8. 可访问与响应式

- 所有图标按钮提供 `aria-label`；频道选择使用 `tablist` / `tab` / `aria-selected`。
- 交互目标至少 44px，键盘焦点使用半透明蓝色 3px 外框。
- 320px 宽下节目卡不得横向溢出；桌面端维持最大 500–560px 阅读宽度。
- Bottom Dock 不能遮挡正文末尾。
- Bottom Dock 的未选中图标使用线形，选中图标使用与页面主色完全一致的蓝色面形，不使用第二种蓝色，也不在选中项上方显示额外指示横线。“每日资讯”、“个人节目”和“我的”均分别使用用户指定的线性 SVG 与选中面性 SVG。
- 不使用 `transition: all` 或长期 `will-change`。

## 9. 验收基线

- `/today` 能加载真实频道与最新节目，三频道可切换。
- 横滑卡片、联动刻度、卡内播放、逐字跟随和收藏行为连贯。
- `/today/programs/[id]` 展示本期来源文章卡片。
- `/today/articles/[id]` 展示站内正文，并对不完整内容提供原链接。
- `/programs` 同时覆盖节目空态、收藏空态、列表态与底部播放抽屉。
- `/account` 展示默认身份和学习数据面板；`/account/settings` 承载退出与删除个人数据。
- 375×812、390×844、320px 小屏和桌面窄栏均无溢出。
- ESLint、生产构建、后端 Ruff 与新闻接口测试通过。
