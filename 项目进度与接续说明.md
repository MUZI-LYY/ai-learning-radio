# AI 学习电台｜项目进度与接续说明

> 更新日期：2026-08-25
>
> 稳定分支：`main`
> 当前发布标签：`v0.3.0-frontend`

## 1. 当前结论

第一阶段产品闭环已经完成，可以进入部署准备阶段。

- 每日资讯：抓取、正文抽取、单 Agent 生成、TTS、来源列表、站内阅读、收藏与逐字跟随已完成。
- 个人节目：上传资料、任务状态、真实生成、节目列表、详情、播放抽屉、知识点和主动回忆已完成。
- 我的：默认个人资料、学习数据、额度、账号设置和删除全部个人数据入口已完成。
- 三个主页面及关键子页面统一使用移动端视觉规范和底部导航规范。

## 2. 第一阶段已完成范围

### 每日资讯

- 三频道循环横滑卡片和同步弧形刻度。
- 节目进度、时间、收藏、播放设置和三个音色试听。
- 音频播放与正文逐字跟随使用同一份节目数据。
- “查看更多”进入本期来源，来源卡片保留图片、两行标题、两行摘要和来源标签。
- “阅读全文”进入站内阅读页，保留原文出口和正文完整性提示。
- 每日资讯能力由唯一的 `LearningRadioAgent` 负责内容判断；来源绑定、预算、抓取和持久化仍由确定性代码负责。

### 个人节目

- 无节目时展示上传引导；有节目时同时展示生成入口和节目列表。
- 节目卡展示生成时间及生成中、已生成、生成失败状态。
- 完成状态才显示可用播放控件；未完成或失败状态按规则禁用。
- 节目卡使用轮换封面，播放/暂停按钮居中覆盖节目图。
- 播放抽屉包含逐字稿和底部播放卡；详情页支持前进/后退 5 秒。
- 详情页展示学习目标、5 个核心知识点和 3 张主动回忆卡。
- 长模型结构化输出支持截断恢复，避免上传后无限 Loading。

### 我的

- 默认头像、昵称“亲爱的用户”和听众说明。
- 展示生成资料、收听节目、收听分钟与当日额度。
- 设置页提供退出登录和删除全部个人数据；删除操作需要明确确认文字。
- 账号设置页已补齐语义化一级标题，视觉样式保持不变。

## 3. 真实生成验收

2026-08-25 使用一份 36 页 PDF 完成真实端到端验收：

- 文件大小：460,482 字节；成功提取 12,071 字符。
- validating、parsing、summarizing、generating、synthesizing 五步均一次成功。
- 总耗时约 2 分 12 秒；生成音频 484 秒。
- 生成 5 个知识点和 3 张主动回忆卡。
- 播放、暂停、前进/后退 5 秒、逐字跟随和抽屉播放均通过。
- 原始上传文件路径已清空并记录删除时间；音频文件存在。
- 本次供应商调用估算成本约 ¥0.7184，实际费用字段等待供应商账单回填。

验收记录只保留步骤、时长、数量、用量与错误码，不在文档中保存 PDF 正文、节目逐字稿、模型原始响应或密钥。

## 4. 发布质量检查

- 后端 Ruff：通过。
- 后端 pytest：56 passed；仅有 Starlette/httpx 既有弃用警告。
- 前端测试：6 passed。
- 前端 ESLint：通过。
- Next.js 生产构建：通过，共 11 个路由。
- `git diff --check`：通过。
- 375px、约 768px、1440px 下三个主页面均无横向溢出和页面错误。
- 每日资讯设置、音色试听、来源阅读；个人节目收藏、播放、详情、主动回忆；账号设置均完成浏览器回归。
- 基础可访问性检查未发现无名称按钮/链接、缺失图片替代文本或重复 ID。

项目尚未建立历史截图基线，因此自动视觉回归对比结论为 **INCONCLUSIVE**；本轮仅确认实时布局测量与交互通过。

## 5. 本地运行

- 前端：`http://127.0.0.1:3001`
- 后端：`http://127.0.0.1:8002`
- 每日资讯：`http://127.0.0.1:3001/today`
- 个人节目：`http://127.0.0.1:3001/programs`
- 我的：`http://127.0.0.1:3001/account`

服务停止后按 README 的本地运行命令重启。真实生成还需要独立 Worker。

## 6. 下一阶段

下一阶段是“部署与小范围真实用户试用”，不再优先扩展新页面：

1. 轮换本地开发期间可能暴露过的 LLM、TTS 和应用凭证。
2. 确定部署平台、域名、HTTPS、进程守护与日志方案。
3. 确定生产数据库和音频对象存储迁移方案，并验证备份恢复。
4. 每个资讯频道配置 2–3 个可信真实 RSS 来源和定时生成策略。
5. 配置月度预算、单任务成本告警和供应商实际账单回填。
6. 建立关键页面截图基线，再启用自动视觉回归。

## 7. 安全提醒

- `.env` 已被 Git 忽略，密钥、邀请码、Cookie 和真实内容不得写进文档或提交。
- IDE 中曾选中疑似凭证片段，部署前必须轮换对应凭证。
- 真实用户删除和退出登录流程在回归测试中保持只读，避免破坏本地验收数据。
- 两份未跟踪的前端说明文档属于用户文件，未自动纳入本次发布提交。
