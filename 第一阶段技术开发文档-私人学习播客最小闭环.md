# 第一阶段技术开发文档｜私人学习播客最小闭环

> 配套文档：《AI学习电台-MVP需求总结》《AI学习电台-架构推导方案》《AI产品Vibe Coding通用技术栈手册》《技术适配声明》
>
> 本文档只覆盖第一阶段。未列入本阶段的能力不得提前实现。

## 一、阶段目标

### 1.1 交付范围

完成一条可在手机浏览器中亲自验证的私人学习播客闭环：

```text
输入邀请码
→ 上传一份 DOCX / 文本型 PDF / Markdown
→ 填写“这次特别想理解什么”
→ 试听并选择 3 个预设音色之一
→ 创建异步任务并显示进度
→ 解析正文并删除原文件
→ 生成教学讲稿、5 个知识点、3 道主动回忆题
→ 合成中文音频
→ 收听、阅读和展开答案
→ 在节目库中再次打开
→ 删除节目或全部个人数据
```

### 1.2 阶段产物

- 可独立启动的 FastAPI API 与 Worker。
- 持久化的用户、邀请码、额度、学习资料、任务步骤、节目和播放事件。
- LLM/TTS 的 mock provider 与真实 provider 接口。
- 3 个预设音色的受控目录和预生成试听音频。
- 最小移动端 PWA：邀请码页、学习上传与音色选择页、任务状态页、节目详情页、最小节目库和数据删除入口。
- Prompt 模板、数据库迁移、自动化测试、真实冒烟脚本与 README。

### 1.3 验收标准

- 合法邀请码可以进入；错误、已作废邀请码不能进入。
- 支持 `.docx`、文本型 `.pdf`、`.md`，单文件不超过 15MB；不支持的格式和扫描 PDF 返回明确原因。
- 提取正文为空或超过约 3 万字时，不进入模型调用，也不消耗额外外部费用。
- 用户提交后立即获得任务 ID；刷新页面、API/Worker 重启后仍能查看并继续任务。
- 用户能试听并选择“儒雅青年 2.0、知性灿灿 2.0、渊博小叔 2.0”；未选择时使用儒雅青年 2.0，后端拒绝白名单外的音色。
- 成功产物包含 5–10 分钟中文单人老师式音频、完整讲稿、正好 5 个知识点、正好 3 道回忆题及答案、原资料名称。
- 生成音频使用任务创建时选定的音色；节目记录保存稳定 `voice_key` 与实际供应商音色快照。
- 原资料内容与 AI 补充说明在文字稿中明确标记；音频遇到补充内容时使用明确口播过渡。
- 原始上传文件解析成功后删除，数据库保留用户私有的提取文本和生成结果。
- 每位用户每天最多创建 2 次私人生成；用户主动“重新生成”消耗额度，系统内部自动重试不重复扣额度。
- TTS 最终失败时保留文字结果，并向用户显示“音频生成失败，可稍后重试”，不能丢失讲稿。
- 用户之间无法读取任务、资料、节目和音频；越权访问统一返回不可枚举的资源错误。
- 未配置月度预算或累计用量达到预算上限时，系统不得继续调用真实 LLM/TTS，并向用户显示可操作的预算暂停提示。
- mock 自动化测试全部通过；真实模型和 TTS 冒烟至少各覆盖一条完整用户链路。

### 1.4 明确不做

- 每日新闻和三个新闻频道。
- 公开文章 URL 解析。
- 扫描 PDF、OCR、旧版 `.doc`、图片/音视频输入、复杂公式和文档图片理解。
- 多文件合并、跨文档分析、开放问答、记忆曲线、自动评分和知识图谱。
- 正式管理员网页后台、公开分享、收藏、评论和下载。
- 自定义声音复刻、任意音色 ID、已生成节目的换音色重合成，以及按用户生成多份新闻音频。
- Supabase/云对象存储和正式公网部署。

## 二、技术适配摘要

- **开发路径**：最小纵向切片；界面只覆盖判断核心价值所需的交互。
- **默认方案**：Python 3.11、FastAPI、Pydantic、pytest、Next.js、TypeScript、Tailwind、`/api/v1`。
- **按需模块**：SQLite、SQLAlchemy、Alembic、数据库任务表、独立 Worker、本地受控文件存储、`python-docx`、`pypdf`、LLM/TTS provider。
- **暂缓项**：PostgreSQL、Supabase Storage、重型任务队列、正式四页 PWA、管理员后台和云部署。
- **编排方式**：确定性状态机；模型不决定流程、不调用任意工具。
- **异步方式**：提交任务 + 前端轮询；不使用 SSE。

## 三、技术栈与模型

| 层 | 本阶段选择 | 说明 |
| --- | --- | --- |
| 后端 | Python 3.11.16、FastAPI、Pydantic | API、校验和业务服务 |
| 数据访问 | SQLAlchemy 2.x、Alembic、SQLite | 保持 PostgreSQL 可迁移性 |
| 文件解析 | `python-docx`、`pypdf`、Markdown 轻量解析 | 只支持 PRD 指定格式 |
| 后台任务 | 数据库任务表 + 独立 Worker 轮询 | 任务状态与步骤结果可恢复 |
| 模型 | 火山方舟 `doubao-seed-2-1-pro-260628`；Provider mock + 真实实现 | 固定版本、关闭深度思考、使用 JSON Schema；模型名、base URL、超时和参数配置化 |
| TTS | 豆包语音 `seed-tts-2.0`；Provider mock + 真实实现 | 异步长文本输出 MP3，失败可降级为文字稿；仅允许 3 个预设音色 |
| 前端 | Next.js、TypeScript、App Router、Tailwind CSS | 移动端最小 PWA 切片 |
| 测试 | pytest；前端类型检查、ESLint 和关键交互测试 | mock 与真实冒烟分层 |

本阶段不引入 RAG、Embedding、OCR、动态 Agent、多 Agent、MCP、Redis、Celery、WebSocket 和 SSE。

## 四、环境与配置

### 4.1 本地运行约定

- 后端：`127.0.0.1:8001`
- 前端：`127.0.0.1:3001`
- Python：使用 Python 3.11，并通过项目级虚拟环境隔离依赖。
- Node：推荐 Node.js 22 LTS；使用更新版本时先验证依赖兼容性。
- 数据和资产：项目根目录下的受控 `data/`，全部加入 `.gitignore`。

### 4.2 配置项

`.env.example` 只写变量名和安全占位值，不包含真实秘密。至少包括：

```text
APP_ENV
APP_SECRET
INVITE_CODE_PEPPER
DATABASE_URL
PRIVATE_STORAGE_ROOT
LLM_PROVIDER
LLM_API_KEY
LLM_BASE_URL
LLM_MODEL_PRIMARY
LLM_THINKING_MODE
LLM_TIMEOUT_SECONDS
TTS_PROVIDER
VOLC_TTS_APP_ID
VOLC_TTS_ACCESS_KEY
VOLC_TTS_BASE_URL
VOLC_TTS_RESOURCE_ID
TTS_DEFAULT_VOICE_KEY
TTS_TIMEOUT_SECONDS
MAX_UPLOAD_MB
MAX_EXTRACTED_CHARS
DAILY_PRIVATE_PROGRAM_LIMIT
PROVIDER_CALLS_ENABLED
PROJECT_MONTHLY_BUDGET_CNY
WORKER_POLL_SECONDS
```

安全要求：

- `.env`、SQLite 数据库、上传文件、音频、缓存和日志不得提交 Git。
- API Key 只由后端/Worker 读取，前端永远不可见。
- 日志只记录文档字符数、哈希、任务 ID、耗时、Token/计费字符数和错误类型，不记录完整私人正文。

## 五、项目结构

```text
AI学习电台/
├── backend/
│   ├── app/
│   │   ├── api/v1/             # auth、learning、tasks、programs、events、me
│   │   ├── core/               # 配置、安全、日志、错误
│   │   ├── models/             # SQLAlchemy 模型
│   │   ├── schemas/            # API 与模型输出结构
│   │   ├── services/
│   │   │   ├── auth/           # 邀请码与会话
│   │   │   ├── documents/      # 文件校验、解析和清理
│   │   │   ├── generation/     # 分块、生成、校验和质量规则
│   │   │   ├── providers/      # LLM/TTS mock 与真实适配器
│   │   │   ├── storage/        # 本地/未来对象存储统一接口
│   │   │   └── prompts/        # 带版本号的 Prompt
│   │   ├── worker/              # 任务领取、步骤执行、恢复和重试
│   │   └── main.py
│   ├── alembic/
│   ├── scripts/                 # 初始化管理员/邀请码、真实冒烟
│   └── tests/
├── frontend/                    # 第一阶段最小 PWA
├── data/                        # 不提交；本地 DB、临时上传和音频
├── docs/                        # 项目决策和阶段文档
├── .env.example
├── .gitignore
└── README.md
```

## 六、数据、资产与状态

### 6.1 数据模型

所有主键使用不可猜测 UUID，时间统一存 UTC。

| 实体 | 关键字段 | 说明 |
| --- | --- | --- |
| `users` | `id`, `display_name`, `role`, `created_at`, `deleted_at` | `role` 当前只允许 `user/admin` |
| `invite_credentials` | `id`, `user_id`, `code_digest`, `revoked_at`, `created_at` | 只保存加 pepper 的摘要，不保存明文邀请码 |
| `sessions` | `id`, `user_id`, `token_digest`, `expires_at`, `revoked_at` | 浏览器只持有 HttpOnly 会话 Cookie |
| `learning_sources` | `id`, `user_id`, `original_name`, `media_type`, `size_bytes`, `text`, `text_sha256`, `raw_path`, `raw_deleted_at`, `created_at` | `raw_path` 解析清理后置空；提取文本为用户私有数据 |
| `generation_tasks` | `id`, `user_id`, `source_id`, `status`, `current_step`, `focus`, `voice_key`, `attempts`, `error_code`, `error_message`, `prompt_version`, `created_at`, `updated_at`, `completed_at` | 长任务主记录；创建时冻结所选音色 |
| `task_steps` | `id`, `task_id`, `step_name`, `status`, `attempts`, `input_hash`, `output_json`, `started_at`, `completed_at` | 记录步骤 journal，支持断点续跑 |
| `programs` | `id`, `user_id`, `source_id`, `task_id`, `title`, `transcript_json`, `voice_key`, `tts_model`, `provider_voice_id`, `audio_key`, `audio_duration_seconds`, `status`, `created_at`, `deleted_at` | 私人节目；保存稳定音色键与供应商快照，音频只存受控对象键 |
| `knowledge_points` | `id`, `program_id`, `position`, `content` | 每期正好 5 条 |
| `recall_questions` | `id`, `program_id`, `position`, `question`, `answer` | 每期正好 3 条，答案由前端默认折叠 |
| `daily_quota_usage` | `user_id`, `usage_date`, `used_count`, `bonus_count` | 创建任务时原子扣减；按 Asia/Shanghai 日期计算 |
| `provider_usage` | `id`, `task_id`, `provider`, `model`, `operation`, `input_units`, `output_units`, `estimated_cost_cny`, `actual_cost_cny`, `created_at` | 记录 LLM/TTS 用量和费用，不保存私人输入原文 |
| `analytics_events` | `id`, `user_id`, `event_name`, `program_id`, `created_at` | 只允许 PRD 定义的必要事件 |

SQLite 开发期也必须开启外键约束。数据库迁移全部由 Alembic 管理，不允许启动时静默重建表。

`generation_tasks` 还需保存创建时选定的 `voice_key`，确保任务恢复和 TTS 重试始终使用同一音色。音色目录使用版本控制下的静态配置，不让客户端直接提交供应商 Voice ID：

| `voice_key` | 展示名 | 豆包语音 Voice ID | 用途 |
| --- | --- | --- | --- |
| `elegant_youth` | 儒雅青年 2.0 | `zh_male_ruyayingnian_uranus_bigtts` | 默认；自然克制的老师式讲解，新闻固定品牌音色 |
| `intellectual_cancan` | 知性灿灿 2.0 | `zh_female_cancan_uranus_bigtts` | 明快女性声线 |
| `erudite_uncle` | 渊博小叔 2.0 | `zh_male_yuanboxiaoshu_uranus_bigtts` | 更有知识播客和故事感 |

火山异步长文本接口使用 `X-Api-App-Id`、`X-Api-Access-Key` 和 `X-Api-Resource-Id: seed-tts-2.0` 请求头；供应商 Voice ID 写入 `req_params.speaker`。这些字段只由服务端 provider 组装，客户端只接触稳定 `voice_key`。

### 6.2 文件生命周期

```text
上传到随机临时路径
→ 校验格式/MIME/大小/文件头
→ 解析为规范化文本
→ 在同一业务步骤中持久化提取文本
→ 删除原始上传文件并记录 raw_deleted_at
→ 生成音频到用户隔离目录
→ 数据删除时删除音频和数据库关联记录
```

- 客户端提供的文件名仅用于展示，不能拼接磁盘路径。
- 解析失败的原文件最多保留到任务进入终态，随后清理；不能形成无人管理的临时文件。
- 音频通过鉴权 API 返回或生成短时访问方式，不能暴露可猜测的公开静态路径。

### 6.3 任务状态机

```text
queued
→ validating
→ parsing
→ summarizing
→ generating
→ synthesizing
→ completed
```

任一步骤发生可重试错误：

```text
当前步骤 → retry_wait → 原步骤
```

不可重试或超过最大重试次数：

```text
当前步骤 → failed
```

特殊降级：教学内容已成功但 TTS 最终失败时，节目状态记为 `text_ready`，任务以带警告的成功终态结束，允许单独重试 TTS。

恢复规则：

- Worker 只领取 `queued`、到期的 `retry_wait` 或租约过期的执行中任务。
- 每个高成本步骤根据 `input_hash + prompt_version/provider/model` 判断是否已有有效结果；已有结果不重复调用。
- 每次真实 Provider 调用前由确定性代码检查 `PROVIDER_CALLS_ENABLED` 和当月预算；达到上限立即熔断，模型不能绕过。
- Provider 调用结束后记录实际用量；供应商未立即返回费用时按配置单价计算保守估值。
- TTS 任务始终从服务端目录把 `voice_key` 映射为供应商 Voice ID；映射失败直接终止，不回退到客户端传入值。
- 任务领取使用数据库条件更新，防止两个 Worker 同时处理。
- 创建任务与扣减额度处于同一事务；系统重试不再扣额度。
- 重启后根据 `task_steps` 从最后一个成功步骤继续。

## 七、文档处理与生成工作流

### 7.1 文件校验和提取

- 文件大小上限：15MB。
- 支持扩展名：`.docx`、`.pdf`、`.md`。
- 同时检查扩展名、声明 MIME、文件签名和解析结果；不因改后缀接受任意文件。
- DOCX 提取标题、正文和简单表格；忽略图片、批注和修订记录。
- PDF 只接受有足够可复制文字的文本型文件。若页数存在但提取文字极少，返回 `SCANNED_PDF_UNSUPPORTED`。
- Markdown 转为保留标题层级的纯文本，不执行 HTML、脚本或远程资源。
- 规范化后正文为空返回 `EMPTY_DOCUMENT`；超过 3 万字返回 `DOCUMENT_TOO_LONG`，不静默截断。

### 7.2 长文分块

- 优先按标题和段落边界切分，每块目标 6000–8000 个中文字符，保留小范围上下文重叠。
- 每块先生成“忠于原文的结构化要点”，包含概念、论据、示例和来源片段位置，不生成最终口播。
- 最终生成只使用提取正文的结构化要点与用户关注点，控制上下文长度。
- 分块结果持久化到 `task_steps`，单块失败只重试该块。
- 任何分块或合并步骤都不得把来源内容误标成 AI 补充，也不得把 AI 补充伪装为原文事实。

### 7.3 教学节目生成

最终结构固定为：

```text
学习目标
→ 背景与概念
→ 分点解释
→ 示例或类比
→ 总结复盘
→ 主动回忆问题
```

建议文字稿目标为约 1400–2600 个中文字符，最终以真实 TTS 音频时长校验 5–10 分钟。首次超出目标不作为解析失败无限重试；最多进行一次确定性的压缩/扩写修订，仍偏离时记录质量警告供产品经理判断。

### 7.4 AI 补充规则

- 默认以原资料内容为主体。
- AI 可以补充帮助理解的通用解释或类比，但必须单独标为 `ai_supplement`。
- 补充内容不得引入具体但不可验证的新事实、数字、人物观点或来源。
- 文字稿显示“AI 补充说明”；音频使用“这里补充一个帮助理解的例子”等明确过渡。

## 八、API 设计

### 8.1 统一约定

- API 前缀：`/api/v1`。
- 登录后使用 HttpOnly、SameSite Cookie；生产环境启用 Secure。
- 成功响应返回受控 schema；错误统一为：

```json
{
  "error": {
    "code": "DOCUMENT_TOO_LONG",
    "message": "提取后的正文超过 3 万字，请缩短后重试。",
    "request_id": "uuid"
  }
}
```

- 所有按 ID 读取或修改的资源都同时按当前 `user_id` 过滤，禁止先读取再在应用层判断归属。

### 8.2 接口清单

| 方法与路径 | 用途 | 关键请求/响应 | 主要错误 |
| --- | --- | --- | --- |
| `POST /auth/invite` | 邀请码登录 | `{invite_code}` → 当前用户摘要；设置会话 Cookie | `INVALID_INVITE`, `REVOKED_INVITE`, `RATE_LIMITED` |
| `POST /auth/logout` | 注销当前会话 | 撤销会话并清 Cookie | `UNAUTHORIZED` |
| `GET /me` | 获取当前用户与今日额度 | 用户摘要、频道占位、剩余额度 | `UNAUTHORIZED` |
| `GET /tts/voices` | 获取私人节目可选音色 | 返回稳定 `voice_key`、展示名、描述、预生成试听 URL 和默认项 | `UNAUTHORIZED` |
| `POST /learning/tasks` | 上传一份资料并创建任务 | multipart：`file`, `focus`, `voice_key` → `{task_id,status,quota_remaining}` | `UNSUPPORTED_FILE`, `FILE_TOO_LARGE`, `INVALID_VOICE`, `QUOTA_EXCEEDED` |
| `GET /tasks/{task_id}` | 查询任务状态 | 状态、当前步骤、可展示进度、错误/警告、节目 ID | `RESOURCE_NOT_FOUND` |
| `POST /tasks/{task_id}/retry-audio` | 对 `text_ready` 结果单独重试 TTS | 新的 TTS 状态，不重复生成讲稿、不扣完整生成额度 | `INVALID_TASK_STATE`, `BUDGET_BLOCKED` |
| `GET /programs` | 获取当前用户私人节目 | 分页节目摘要 | `UNAUTHORIZED` |
| `GET /programs/{program_id}` | 获取节目详情 | 讲稿、知识点、回忆题、来源和音频状态 | `RESOURCE_NOT_FOUND` |
| `GET /programs/{program_id}/audio` | 鉴权读取音频 | 支持 Range 的音频响应 | `AUDIO_NOT_READY`, `RESOURCE_NOT_FOUND` |
| `POST /programs/{program_id}/regenerate` | 用户主动重新生成 | 创建新任务并扣一次当日额度 | `QUOTA_EXCEEDED`, `RESOURCE_NOT_FOUND` |
| `DELETE /programs/{program_id}` | 删除单期节目及音频 | 删除结果 | `RESOURCE_NOT_FOUND` |
| `DELETE /me/data` | 删除用户全部私人资料和生成结果 | 需提交二次确认短语；保留最小审计记录 | `CONFIRMATION_REQUIRED` |
| `POST /events` | 上报必要匿名事件 | 只允许白名单事件及关联节目 | `INVALID_EVENT` |

第一阶段不提供“手动重跑整个失败任务且免费”的通用接口。系统故障由自动重试处理；需要用户修正输入时应创建新任务并明确额度行为，避免语义不清。

### 8.3 事件白名单

- `invite_verified`
- `program_play_started`
- `program_play_50_percent`
- `program_play_completed`

同一用户、节目和关键进度事件需要幂等去重，防止播放器频繁触发造成指标失真。

## 九、Prompt 与模型输出设计

### 9.1 Prompt 文件

- `chunk_summary_v1.md`：把单个原文分块整理为忠实要点，不补充外部知识。
- `lesson_generation_v1.md`：根据全部块要点和用户关注点生成教学节目结构。
- `lesson_revision_v1.md`：只在时长或结构明显不合格时执行一次受控修订。

第一阶段上述三个节点统一使用 `doubao-seed-2-1-pro-260628`，并关闭深度思考。核心链路稳定后，才评估把分块要点节点切换为 `doubao-seed-2-0-lite-260428`；该降本路由不属于第一阶段。

每个 Prompt 包含：角色、允许使用的输入、正面格式示例、禁止格式示例、数量约束、事实边界和输出 JSON schema。

### 9.2 结构化结果

LLM 最终输出经 Pydantic 校验，核心结构如下：

```json
{
  "title": "节目标题",
  "learning_objectives": ["目标一"],
  "segments": [
    {
      "section": "背景与概念",
      "origin": "source",
      "narration": "口播内容"
    },
    {
      "section": "示例或类比",
      "origin": "ai_supplement",
      "narration": "明确标记的补充说明"
    }
  ],
  "summary": "总结复盘",
  "knowledge_points": ["正好五条"],
  "recall_questions": [
    {"question": "问题", "answer": "答案"}
  ]
}
```

确定性校验包括：

- `knowledge_points` 数量必须等于 5。
- `recall_questions` 数量必须等于 3，问题和答案不能为空。
- `segments.origin` 只允许 `source` 或 `ai_supplement`。
- 必需教学章节不能缺失。
- 所有文字长度存在合理上限，防止异常输出拖垮 TTS 和存储。

解析器兼容模型常见的 Markdown 代码围栏，但不把数量超标简单当成 JSON 解析失败。可确定安全裁剪的列表由后端裁剪并记录质量警告；事实或结构不完整才触发有限重试。

## 十、前端最小纵向切片

### 10.1 页面

1. **邀请码页**：输入、验证中、错误和成功进入。
2. **学习页**：选择文件、展示限制、填写关注点、试听并选择 3 个预设音色、显示今日剩余额度、提交。
3. **任务页**：展示当前步骤、自动轮询、失败原因、文字就绪但音频失败的降级状态。
4. **节目详情页**：播放器、来源名称、文字稿、5 个知识点、3 道答案默认折叠的回忆题。
5. **最小节目库/账户入口**：查看历史私人节目、删除单期节目、删除全部个人数据、退出。

### 10.2 交互底线

- 以 375px 左右手机宽度优先设计，并检查常见移动端与桌面宽度。
- 上传中、排队、生成中、文字已就绪、完成、失败、空数据和离线恢复均有明确状态。
- 音色试听使用预生成短音频，不发起实时 TTS；同一时间最多播放一段试听。
- 播放器支持播放、暂停、拖动、继续播放和 HTTP Range。
- 页面刷新后根据任务 ID 恢复，不依赖内存状态。
- 删除全部数据必须二次确认；完成后清理会话并回到邀请码页。
- 第一阶段只做 PWA 基础清单和可添加主屏能力；锁屏媒体控制在真机可用性验证中记录结果，不为兼容问题引入原生 App。

## 十一、测试要求

### 11.1 第一层：mock 自动化测试

后端至少覆盖：

- 正确、错误、已作废邀请码和登录限流。
- 会话过期、注销和跨用户资源访问。
- 三种合法文件解析；错误扩展名、伪造 MIME、超 15MB、空文档、扫描 PDF、超 3 万字。
- 安全文件名、路径遍历和临时文件清理。
- 创建任务与额度扣减的事务性；每日两次限制、奖励额度和系统重试不重复扣减。
- 每个任务状态转换、非法转换、Worker 租约、进程重启恢复和重复领取幂等。
- LLM 合法 JSON、代码围栏、常见等价格式、数量异常、缺字段、超时、限流和重试耗尽。
- TTS 成功、失败重试、降级为 `text_ready` 和单独恢复音频。
- 3 个合法 `voice_key`、缺省音色、伪造音色、任务恢复后音色不变，以及 `voice_key` 到供应商 ID 的白名单映射。
- 试听文件存在且可播放，点击试听不创建 TTS 用量记录。
- Provider 调用关闭、预算未配置、预算将超限和已达上限时均在调用前熔断；并验证用量账本不重复记账。
- 原文件成功解析后删除；节目/全部数据删除时数据库与音频一致清理。
- 播放事件白名单与幂等去重。
- API 统一错误结构不泄露堆栈和内部路径。

前端至少运行：

- TypeScript 类型检查和 ESLint。
- 邀请码、上传、轮询、失败降级、播放器、答案折叠和删除确认的关键交互测试。
- 375px 手机宽度和桌面宽度下的布局检查。

### 11.2 第二层：真实模型和 TTS 冒烟

使用一份不含敏感信息且允许发送给第三方的真实 Markdown 或 DOCX：

1. 输入真实邀请码并上传文件。
2. 使用火山方舟 `doubao-seed-2-1-pro-260628` 跑完分块要点和最终教学生成，确认深度思考关闭且 JSON Schema 生效。
3. 校验 5 个知识点、3 道回忆题、来源/补充标记及内容质量。
4. 分别用 3 个白名单音色完成一次短试听合成，再用所选默认音色完成整期真实 TTS，并在手机浏览器播放。
5. 刷新页面和重启 Worker，确认任务/节目仍可读取。
6. 记录供应商、模型/音色、Prompt 版本、输入字符数、Token/计费字符数、各步骤耗时、重试次数、估算成本、音频时长和人工质量结论。

不得把 mock 音频或 mock 模型结果标记为真实冒烟通过。

## 十二、产品经理验收清单

- [ ] 在手机浏览器打开本地或测试地址，输入正确邀请码能进入，错误邀请码有明确提示。
- [ ] 上传一个合法 DOCX/PDF/Markdown，页面能看见格式和 15MB 限制。
- [ ] 三个预设音色都能试听，切换试听时不会同时播放两段音频。
- [ ] 填写“特别想理解什么”、选择音色并提交，页面立即显示任务已创建和剩余额度。
- [ ] 生成过程中刷新页面，仍能看到同一任务及当前步骤。
- [ ] 生成完成后能播放、暂停、拖动并继续播放音频。
- [ ] 节目时长在 5–10 分钟范围内，讲解像单人老师授课而不是照读原文。
- [ ] 生成节目使用提交时选择的音色，节目详情能看到该音色名称。
- [ ] 文字稿包含固定教学结构，AI 补充内容能被明确识别。
- [ ] 页面正好显示 5 个核心知识点和 3 道主动回忆题，答案默认折叠且可以展开。
- [ ] 节目详情能看到原资料名称。
- [ ] 返回节目库后能再次打开刚生成的节目。
- [ ] 连续生成达到每日额度后，第三次提交被阻止并说明原因。
- [ ] 上传不支持或不可解析的文件，页面说明具体失败原因，不出现空白页。
- [ ] 模拟 TTS 失败后仍能看到文字稿，并能单独重试音频。
- [ ] 删除单期节目后无法再次访问其文字和音频。
- [ ] 执行“删除全部个人数据”并二次确认后，节目、资料和音频均不可访问。

## 十三、风险与待确认项

### 13.1 已知风险

- PDF 文本提取质量会随排版变化；复杂双栏和公式文档可能可提取但阅读顺序不理想。
- 分块摘要可能遗漏细节；需用真实长文测试生成忠实度，而不是只验证 JSON 格式。
- TTS 实际语速决定节目时长，不能只按文字数判断。
- 系统音色可能被供应商升级或下线，因此节目需保存实际 Voice ID 快照，音色目录变更必须回归试听；既有音频不重新合成。
- SQLite 只适合第一阶段本地验证；正式多用户上线前必须迁移并验证数据库并发、RLS 和私有音频访问。
- 邀请码等同 MVP 登录凭证，泄露后可访问个人资料，因此必须足够长、可撤销并配合登录限流。

### 13.2 真实验收前必须确认

1. 一份允许发送给第三方服务的真实测试资料。
2. 本项目的月度测试预算上限；未配置前保持真实 Provider 调用关闭。

### 13.3 本阶段无需提前确认

- 新闻源、新闻频道生成规则的工程细节。
- 正式部署平台、域名和 Supabase 配置。
- 管理员网页后台的交互设计。

## 十四、交接给下一阶段

第一阶段验收通过后，可直接复用：

- 邀请码认证、用户隔离、额度与删除机制。
- 持久化任务状态机、Worker、重试、日志和成本记录。
- LLM/TTS provider、Prompt 版本和结构化校验框架。
- 私有音频存储接口、播放器和节目库数据契约。
- 私人节目 3 音色目录、试听组件和服务端白名单校验；新闻阶段固定复用 `elegant_youth`，不按用户重复合成。
- 移动端 PWA 骨架。

第二阶段在此基础上新增公开文章解析与每日新闻管线；不得复制一套平行任务系统或播放器。
